name: echo # 插件唯一识别名，以 astrbot_plugin_ 前缀开头
display_name: Echo # 用于展示的名字，可以是方便人类阅读的名字（需要版本 >= v4.5.0，低版本不会报错，请放心填写）
desc: 接收用户信息，并返回相同的信息。 # 插件简短描述
version: v0.0.1 # 插件版本号。格式：v1.1.1 或者 v1.1
author: R2m1liA # 作者
repo: https://github.com/R2m1liA/astrbot_plugin_echo # 插件的仓库地址
